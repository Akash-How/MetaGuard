# Shared Contracts

This directory is reserved for cross-stack contracts once the frontend and backend begin sharing typed payload definitions more formally.

Possible next steps:

- add OpenAPI-generated client artifacts
- add JSON schema snapshots for critical responses
- add sample payloads for frontend mocking

