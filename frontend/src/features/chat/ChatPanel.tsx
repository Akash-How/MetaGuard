import { Panel } from "../../components/ui/Panel";

export function ChatPanel() {
  return (
    <Panel
      title="Metadata Chat"
      description="Provide a lightweight conversational layer for metadata lookup and guided analysis."
    >
      <span className="badge">Optional scaffold included</span>
    </Panel>
  );
}

