import { Panel } from "../../components/ui/Panel";

export function DataPassportPanel() {
  return (
    <Panel
      title="Data Passport"
      description="Generate business-friendly documentation and trust context for a selected data asset."
    >
      <span className="badge">Claude integration pending</span>
    </Panel>
  );
}

